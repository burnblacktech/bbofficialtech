# NPM Configuration for BurnBlack Platform
# Security and performance optimizations

# Security Settings
audit-level=high
ignore-scripts=false

# Development Dependencies
# Note: Some dev dependencies have known vulnerabilities that are safe
# because they only run during development, not in production.
# See: security_vulnerability_analysis.md for details

# Performance
prefer-offline=false
progress=true

# Registry
registry=https://registry.npmjs.org/

# Logging
loglevel=warn
