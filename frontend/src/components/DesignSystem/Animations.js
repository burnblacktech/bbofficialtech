// Stub - Animations
export const PageTransition = ({ children }) => children;
export const FadeInUp = ({ children }) => children;
export const StaggerContainer = ({ children }) => children;
export const StaggerItem = ({ children }) => children;
export const ModalTransition = ({ children }) => children;

export default {
    PageTransition,
    FadeInUp,
    StaggerContainer,
    StaggerItem,
    ModalTransition,
};
