// Stub — document service removed for MVP
const documentService = {
  getDocuments: async () => ({ data: [] }),
  uploadDocument: async () => ({}),
  deleteDocument: async () => ({}),
  downloadDocument: async () => ({}),
};
export default documentService;
